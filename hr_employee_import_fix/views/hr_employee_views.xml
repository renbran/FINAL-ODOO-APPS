<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Employee Import Wizard Form View -->
    <record id="view_hr_employee_import_wizard_form" model="ir.ui.view">
        <field name="name">hr.employee.import.wizard.form</field>
        <field name="model">hr.employee.import.wizard</field>
        <field name="arch" type="xml">
            <form string="Import Employees">
                <div class="alert alert-info" role="alert">
                    <strong>Employee Import Helper</strong><br/>
                    This wizard will safely import employees and handle foreign key constraint issues automatically.
                    Missing resource records will be created automatically.
                </div>
                <group>
                    <group>
                        <field name="import_file" widget="binary" filename="filename"/>
                        <field name="filename" invisible="1"/>
                    </group>
                    <group>
                        <field name="skip_errors"/>
                    </group>
                </group>
                <footer>
                    <button name="import_employees" string="Import Employees" type="object" class="btn-primary"/>
                    <button string="Cancel" class="btn-secondary" special="cancel"/>
                </footer>
            </form>
        </field>
    </record>

    <!-- Employee Import Wizard Action -->
    <record id="action_hr_employee_import_wizard" model="ir.actions.act_window">
        <field name="name">Import Employees</field>
        <field name="res_model">hr.employee.import.wizard</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
    </record>

    <!-- Menu Item -->
    <menuitem 
        id="menu_hr_employee_import_wizard"
        name="Import Employees (Safe)"
        parent="hr.menu_hr_employee"
        action="action_hr_employee_import_wizard"
        sequence="10"/>

    <!-- Enhanced Employee Form View with Resource Info -->
    <record id="view_employee_form_enhanced" model="ir.ui.view">
        <field name="name">hr.employee.form.enhanced</field>
        <field name="model">hr.employee</field>
        <field name="inherit_id" ref="hr.view_employee_form"/>
        <field name="arch" type="xml">
            <field name="company_id" position="after">
                <field name="resource_id" readonly="1" groups="base.group_no_one"/>
            </field>
        </field>
    </record>

    <!-- Fix Orphaned Employees Action -->
    <record id="action_fix_orphaned_employees" model="ir.actions.server">
        <field name="name">Fix Orphaned Employees</field>
        <field name="model_id" ref="hr.model_hr_employee"/>
        <field name="state">code</field>
        <field name="code">
result = model.fix_orphaned_employees()
action = {
    'type': 'ir.actions.client',
    'tag': 'display_notification',
    'params': {
        'title': 'Fix Complete',
        'message': result['message'],
        'type': 'success',
        'sticky': True,
    }
}
        </field>
    </record>

    <!-- Add Fix Orphaned Employees to HR Menu -->
    <menuitem 
        id="menu_fix_orphaned_employees"
        name="Fix Orphaned Employees"
        parent="hr.menu_hr_employee"
        action="action_fix_orphaned_employees"
        sequence="100"/>

</odoo>
