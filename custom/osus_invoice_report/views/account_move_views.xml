<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Inherit account move form view to add custom print buttons -->
    <record id="view_move_form_inherit_osus" model="ir.ui.view">
        <field name="name">account.move.form.inherit.osus.reports</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_move_form"/>
        <field name="arch" type="xml">
            <!-- Only add print buttons, do NOT add duplicate fields -->
            <xpath expr="//header" position="inside">
                <button name="action_print_custom_invoice" 
                        string="Print OSUS Invoice"
                        class="btn-secondary"
                        type="object"
                        groups="account.group_account_invoice"
                        invisible="move_type not in ('out_invoice', 'out_refund')"/>
                
                <button name="action_print_custom_bill" 
                        string="Print OSUS Bill"
                        class="btn-secondary"
                        type="object"
                        groups="account.group_account_invoice"
                        invisible="move_type not in ('in_invoice', 'in_refund')"/>
                
                <button name="action_print_custom_receipt" 
                        string="Print Receipt"
                        class="btn-secondary"
                        type="object"
                        groups="account.group_account_invoice"/>
            </xpath>
        </field>
    </record>

    <!-- Inherit account move tree view to show deal information -->
    <record id="view_move_tree_inherit_osus" model="ir.ui.view">
        <field name="name">account.move.tree.inherit.osus</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_move_tree"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='partner_id']" position="after">
                <field name="buyer" optional="hide"/>
                <field name="project" optional="hide"/>
                <field name="unit" optional="hide"/>
                <field name="deal_id" optional="hide"/>
            </xpath>
            <xpath expr="//field[@name='invoice_date']" position="after">
                <field name="booking_date" optional="hide"/>
            </xpath>
            <xpath expr="//field[@name='amount_total_signed']" position="after">
                <field name="sale_value" optional="hide"/>
                <field name="developer_commission" widget="percentage" optional="hide"/>
            </xpath>
        </field>
    </record>

    <!-- Search view for account moves with deal information -->
    <record id="view_account_move_filter_inherit_osus" model="ir.ui.view">
        <field name="name">account.move.select.inherit.osus</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_account_move_filter"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='partner_id']" position="after">
                <field name="buyer"/>
                <field name="project"/>
                <field name="unit"/>
                <field name="deal_id"/>
            </xpath>
            <xpath expr="//group[@expand='0']" position="inside">
                <filter string="Buyer" name="group_buyer" domain="[]" context="{'group_by': 'buyer'}"/>
                <filter string="Project" name="group_project" domain="[]" context="{'group_by': 'project'}"/>
                <filter string="Unit" name="group_unit" domain="[]" context="{'group_by': 'unit'}"/>
                <filter string="Booking Date" name="group_booking_date" domain="[]" context="{'group_by': 'booking_date'}"/>
            </xpath>
        </field>
    </record>

    <!-- Custom Invoice Report Action -->
    <!-- Duplicate report actions removed: see main osus_invoice_report/views/account_move_views.xml -->

    <!-- Modern Custom Invoice Report Action -->
    <record id="action_report_custom_invoice_modern" model="ir.actions.report">
        <field name="name">OSUS Modern Tax Invoice</field>
        <field name="model">account.move</field>
        <field name="report_type">qweb-pdf</field>
        <field name="report_name">osus_invoice_report.report_custom_invoice_modern</field>
        <field name="report_file">osus_invoice_report.report_custom_invoice_modern</field>
        <field name="binding_model_id" ref="account.model_account_move"/>
        <field name="binding_type">report</field>
        <field name="paperformat_id" ref="osus_invoice_report.paperformat_osus_invoice"/>
        <field name="print_report_name">'OSUS-Modern-Invoice-%s' % (object.name or 'draft')</field>
        <field name="groups_id" eval="[(4, ref('account.group_account_invoice'))]"/>
    </record>
</odoo>