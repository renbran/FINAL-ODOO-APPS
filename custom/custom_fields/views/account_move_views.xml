<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <!-- Account Move (Invoice) Form View -->
        <record id="view_move_form_inherit_custom_fields" model="ir.ui.view">
            <field name="name">account.move.form.inherit.custom.fields</field>
            <field name="model">account.move</field>
            <field name="inherit_id" ref="account.view_move_form"/>
            <field name="arch" type="xml">
                <!-- Add Deal Information Tab -->
                <xpath expr="//notebook" position="inside">
                    <page string="Deal Information" name="deal_info" invisible="move_type not in ('out_invoice', 'out_refund')">
                        <group>
                            <group string="Customer &amp; Deal Details">
                                <field name="booking_date"/>
                                <field name="buyer_id"/>
                                <field name="deal_id"/>
                            </group>
                            <group string="Project &amp; Financial Information">
                                <field name="project_id"/>
                                <field name="unit_id"/>
                                <field name="sale_value"/>
                                <field name="developer_commission" widget="percentage"/>
                            </group>
                        </group>
                    </page>
                </xpath>
            </field>
        </record>
    </data>
</odoo>