<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Override search view to fix undefined field error -->
    <record id="hr_expense_view_search_fix" model="ir.ui.view">
        <field name="name">hr.expense.view.search.fix</field>
        <field name="model">hr.expense</field>
        <field name="inherit_id" ref="hr_expense.hr_expense_view_search"/>
        <field name="priority">99</field>
        <field name="arch" type="xml">
            <!-- This view just ensures proper inheritance priority -->
            <!-- The actual fix might be removing a problematic field added by another module -->
        </field>
    </record>
</odoo>
