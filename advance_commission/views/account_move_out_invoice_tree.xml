<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Inherit the customer invoice tree view to add deal info fields -->
    <record id="view_out_invoice_tree_commission_deal" model="ir.ui.view">
        <field name="name">account.move.out_invoice.tree.commission.deal</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_out_invoice_tree"/>
        <field name="arch" type="xml">
            <xpath expr="//tree" position="inside">
                <field name="deal_id_display" string="Deal ID" readonly="1"/>
                <field name="booking_date" string="Booking Date"/>
                <field name="buyer_id" string="Buyer Name"/>
                <field name="project_id" string="Project"/>
                <field name="unit_id" string="Unit"/>
                <field name="sale_value" string="Sale Value"/>
            </xpath>
        </field>
    </record>
</odoo>
